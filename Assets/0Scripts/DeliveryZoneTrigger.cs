using Unity.VisualScripting;
using UnityEngine;

public class DeliveryZoneTrigger : MonoBehaviour
{
    // [SerializeField] private LevelManager manager;


    /*private void OnTriggerStay(Collider other)
    {
        // Check if grabbable and input
        if (other.gameObject.layer == 6 && Input.GetKeyDown(KeyCode.E))
        {
            manager.DeliverOrder(order.gameObject);
            // Destroy(other.gameObject, destroyDelay);
        }
    }*/
}
